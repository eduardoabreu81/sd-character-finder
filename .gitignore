# ============================================
# Documentação privada de contexto para IA
# ============================================
.github/copilot-instructions.md
AGENTS.local.md
# ``docs/*`` (not ``docs/``) so the public documents below can be re-included.
# PROJECT_LOG.md stopped being versioned on 2026-08-29; it is agent context,
# not a public document. CATALOG_REBUILD.md stays tracked because the README
# links to it.
docs/*
!docs/CATALOG_REBUILD.md
.specstory/
.codegraph/

# ============================================
# Local runtime artifacts
# ============================================
__pycache__/
*.py[cod]
tmp_resolve_*.txt
data/covers/
data/artist_covers/
# The AnimaDex character export is a tracked rebuild input; the artist
# export is not needed to rebuild the character catalogue.
data/anima_import/*
!data/anima_import/characters.csv
data/anima_preview_samples/
data/anima_characters.db
data/characters.db.download
data/catalog/*.download
data/runtime/
*.db.backup_*
data/artist_favorites.json
data/favorites.json
data/recent_viewed.json
data/search_history.json
data/user_overrides.json
data/user_overrides_v2.json

# IDE / editor
.vscode/
*.ipynb

# Test scripts
test_df.py

AGENTS.md

# Generated catalogue staging
data/generated/
